{
  "private": true,
  "name": "converge",
  "description": "Extensible command-line Twitch bot.",
  "repository": "https://github.com/citycide/converge",
  "workspaces": [
    "core",
    "plugins/*",
    "types"
  ],
  "scripts": {
    "build": "yarn workspace @converge/core build",
    "dev": "yarn workspace @converge/core dev",
    "start": "yarn workspace @converge/core start",
    "lint": "eslint \"{core,plugins}/**/*.js\" --ignore-path .gitignore"
  },
  "dependencies": {
    "typescript": "^3.8.0-beta"
  }
}
