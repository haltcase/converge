{
  "private": true,
  "name": "converge",
  "description": "Extensible command-line Twitch bot.",
  "repository": "https://github.com/citycide/converge",
  "workspaces": [
    "core",
    "plugins/*",
    "types"
  ],
  "scripts": {
    "dev": "cd core && yarn dev",
    "start": "cd core && yarn start",
    "lint": "eslint \"{core,plugins}/**/*.js\" --ignore-path .gitignore"
  },
  "dependencies": {
    "typescript": "^3.8.0-beta"
  }
}
